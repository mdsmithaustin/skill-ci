name: test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reusable-workflow-contract:
    uses: ./.github/workflows/skill-checks.yml
    with:
      skills-dir: .github/fixtures/skills
      evals-dir: .github/fixtures/evals
      require-manifests: true
      skill-ci-ref: ffffffffffffffffffffffffffffffffffffffff
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: jdx/mise-action@5228313ee0372e111a38da051671ca30fc5a96db
        with:
          version: 2026.8.2
      - run: mise run test
